Perform a read-only diagnostic analysis of factual-source coverage for the canonical category:

מבוא

The purpose is to determine whether מבוא is genuinely a limited-content category compared with the other 19 canonical categories, before we make any changes to retry policy, category policy, or WP-021.

This is NOT an implementation WP.

Do not modify:
- source code;
- prompts;
- configuration;
- category mappings;
- retrieval parameters;
- chunking;
- validators;
- generation policy;
- attempt limits.

Do not make any OpenAI/LLM/API calls.
Do not commit anything.

Use only the existing extracted/chunked factual corpora and existing WP-006 TF-IDF retrieval infrastructure.

## 1. Compare all canonical categories

Use the current canonical-category source programmatically.

For each canonical category, run the same deterministic retrieval procedure against the student-summary corpus.

Use exactly the existing production retrieval configuration.

Do not special-case מבוא during retrieval.

For each category report:

- category name;
- top-k results returned;
- retrieval scores;
- number of distinct chunks represented;
- number of distinct physical pages represented;
- number of distinct source files represented;
- min / max / average retrieval score;
- score of the top result;
- score of the lowest returned positive result.

Present this as a comparison table for all 20 categories.

## 2. Analyze מבוא in detail

For מבוא, inspect the actual retrieved chunks rather than relying only on numeric scores.

Report:

- chunk IDs;
- source files;
- physical page numbers;
- retrieval scores;
- short factual/topic summary of each retrieved chunk;
- apparent distinct concepts represented across the retrieved evidence.

Do not use outside neuroanatomy knowledge to decide what ought to be present.

Judge coverage only from what actually exists in the project's factual source corpus.

## 3. Compare evidence diversity

Estimate evidence diversity for each category using deterministic/source-derived signals where possible.

Consider:

- number of distinct pages;
- number of distinct source documents;
- whether top results concentrate heavily on one page;
- whether several retrieved chunks contain substantially overlapping material;
- whether the retrieved evidence appears to cover multiple distinct concepts.

Do not introduce embeddings, another LLM, or a new semantic-analysis system just for this diagnostic.

For qualitative inspection, read the retrieved text directly.

Specifically compare מבוא against:

- the median category;
- several clearly content-rich categories;
- the category with the next-lowest apparent coverage.

## 4. Investigate the WP-006 observation

WP-006 previously reported that מבוא had the weakest canonical-category retrieval result, with a top score around:

0.088

Verify the current result using the existing retrieval implementation.

Determine whether this weak score appears to be caused by:

- the category name being short/generic;
- genuinely sparse source material;
- evidence being present but using terminology other than מבוא;
- concentration of evidence in very few pages/chunks;
- some combination of these.

Do not change the query to improve the result.

You may inspect the retrieved corpus to explain what the existing query is actually finding.

## 5. Assess support for question diversity

The practical question is:

Does the factual source material for מבוא appear sufficient to support 2 distinct, well-grounded exam questions without forcing near-duplicates or repeatedly testing the same fact?

Answer this based only on the available corpus.

Classify מבוא as one of:

A. NORMAL COVERAGE
Evidence breadth appears comparable enough to ordinary categories.

B. LIMITED BUT SUFFICIENT FOR 2 QUESTIONS
Clearly less depth/diversity than typical categories, but there appears to be enough distinct factual material for two questions.

C. TOO LIMITED FOR RELIABLE 2-QUESTION GENERATION
Available evidence is so narrow/repetitive that requesting two distinct questions is likely to create duplication or quality problems.

Do not change anything based on this classification.

## 6. Reliability statistics

Do not remove מבוא from previous measurements.

Instead explain how future evaluation should report:

1. all 20 canonical categories;
2. the 19 categories excluding מבוא;
3. מבוא separately as a potentially limited-content category.

Using the already-collected 20-category reliability sample, recalculate the attempt-distribution statistics excluding מבוא.

Do not rerun generation.

Use the existing results only.

Report at minimum:

- accepted questions;
- operational failures;
- exhaustion count;
- accepted on attempt 1;
- accepted on attempt 2;
- accepted on attempt 3;
- average attempts per accepted question.

Show both:

ALL 20 CATEGORIES
vs.
EXCLUDING מבוא

Do not hide the full 20-category result.

## 7. Important separation

Do not attribute unrelated failures to מבוא.

In particular, the previously observed:

InvalidGroundingOutputError

occurred for:

התעלה השדרתית ותכולתה

That remains a separate system-level reliability issue.

This diagnostic is only intended to determine whether מבוא deserves special treatment because of source-content limitations.

## 8. Final report

Give me a concise report containing:

1. all-category retrieval comparison table;
2. detailed מבוא evidence analysis;
3. comparison of מבוא with typical/content-rich categories;
4. explanation of why its retrieval score is unusually weak;
5. assessment of factual-content breadth;
6. assessment of whether two distinct questions are realistically supported;
7. reliability metrics with and without מבוא;
8. one of the three classifications:
   - NORMAL COVERAGE
   - LIMITED BUT SUFFICIENT FOR 2 QUESTIONS
   - TOO LIMITED FOR RELIABLE 2-QUESTION GENERATION

Finally state which policy options the evidence would support, WITHOUT implementing or choosing one:

- keep מבוא identical to other categories;
- keep it but treat it as a limited-content category;
- request fewer questions from it;
- give it a different attempt budget;
- reconsider whether it should remain an independent canonical category.

Do not recommend changes to prompts, retry limits, category structure, or architecture yet.

Wait for architect/user review.
